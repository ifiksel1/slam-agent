.DS_Store
*.pyc
__pycache__/
*.bag
*.log
mcp/__pycache__/
*.yaml.tmp

# Vendored Docker build sources (cloned by scripts/fetch_docker_sources.sh)
docker_src/

# Local machine-specific compose override (USB mount paths, etc.)
docker-compose.override.yml

# Large flight logs / dataflash .bin (4.7GB) - never commit
logs/

# LI-Init calibration outputs (results + logs, regenerated per run)
li_init_result/
