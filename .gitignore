.DS_Store
*.pyc
__pycache__/
*.bag
*.log
mcp/__pycache__/
*.yaml.tmp

# Vendored Docker build sources (cloned by scripts/fetch_docker_sources*.sh)
docker_src/
docker_src_fastlivo2/

# Local tooling artifacts (machine-specific, not project content)
.catkin_tools/
.claude/projects/
superodom_integration/**/*.db3

# Shared agent-memory worktree (checkout of the 'agent-memory' branch; per-machine,
# registered locally via scripts/agent_memory.sh). The MEMORY lives on its branch, not here.
.agent-memory/
