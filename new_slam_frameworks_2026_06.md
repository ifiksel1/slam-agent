---
name: new-slam-frameworks-2026-06
description: EllipseLIO + SuperOdom added to slam-integration agent (un-validated); ArduPilot→PX4 migration under consideration for MRS UAV stack
metadata: 
  node_type: memory
  type: project
  originSessionId: 957e3751-78b0-4a42-987f-34e5eb506fb3
---

Added two ROS 2 LiDAR-inertial frameworks to the slam-integration agent on 2026-06-08, plus a PX4 migration scoping doc.

**EllipseLIO** (`ellipse_lio`, repo v4rl-ucy/ellipselio) — adaptive ellipsoid LIO, FAST-LIO2 lineage (vendored IKFoM/i-Octree, no GTSAM/Ceres). ROS 2 Humble+Jazzy. Output `/ellipselio_odom`. Lighter build; claimed better in confined spaces. **Try this first.**

**SuperOdom** (`super_odometry`, repo v4rl-ucy/SuperOdom) — LIO with 6-DOF degeneracy/uncertainty. ROS 2 Humble only. Heavy build (source GTSAM@4abef92 with `MARCH_NATIVE=OFF`, Sophus@97e7161, livox driver is a hard dep). Output `/state_estimation` + health gate `/state_estimation_health`. Note: odom covariance NOT populated and degeneracy_threshold params inert. ⚠️ Neither health NOR uncertainty is trustworthy (see 5× sweep below) — gate externally on pose-stream kinematic plausibility.

Files touched: `scripts/install_slam_algorithm.sh`, `docs/phases/phase2_validation.md`, `docs/phases/phase3_generation.md`, `docs/profiles/jetson_orin-ouster_os1_64-{ellipse_lio,super_odometry}-ardupilot-humble.yaml`, `.claude/skills/slam-integration/references/frameworks_ellipse_super.md`, SKILL.md KB table.

**Rig reality (verified 2026-06-09, corrects earlier notes):** Orin NX **8GB / 6-core** on **CTI Hadron** carrier, **Ubuntu 20.04 (focal) — ROS Noetic only, NO native Humble**. All ROS 2 work is in Docker (`ros:humble`, `ros2-jetson-humble`, `slam-gpu` images). The curated FAST-LIO2 profile's "16GB/8-core/22.04" was WRONG. ⚠️ `~/super_ws` = the **SUPER planner** (HKU, phase8b), NOT SuperOdometry — name collision.

**SuperOdom status (2026-06-09): Phase 5 BENCH+MOTION VALIDATED on the real rig.** Built `superodom:humble` (arm64) and ran live ROS2 Ouster driver + SuperOdom in one Humble container. `/state_estimation` 25Hz, health true, valid TF; hand-carried motion tracked, left-90° measured −91°, ~0.13m loop-closure. Working setup committed to `superodom_integration/` (Dockerfile, configs, driver params, bringup script, sampler, motion log). Both frameworks are camera-free LIO (sidesteps the FAST-LIVO2 camera blocker).

**Rig facts confirmed live:** Ouster sensor `192.168.2.60`, udp_dest/host `192.168.2.50`, 1024x20 LEGACY, OS-1-64-U13 fw 2.4.0. FAST-LIVO2 was a docker-compose container (`docker-compose.fastlivo2.yml`, restart=unless-stopped) — **stopped + disabled (restart=no), user wants it left OFF**; SuperOdom is now the active stack. It feeds the Ouster, so only one driver at a time.

**SuperOdom build gotchas (all fixed in superodom_integration/Dockerfile):** `osrf/ros:humble-desktop-full` is amd64-only → built under QEMU; use `ros:humble-ros-base` (arm64) + `--platform linux/arm64`. ouster-sdk needs `libzip-dev`. Livox `build.sh` `rm -rf`s install/ + full colcon build → do ONE unified `colcon build` (no --packages-skip) with `-DROS_EDITION=ROS2`. torch/numba/scikit pip lines dropped (no aarch64 wheels).

**EXTRINSIC FIX (2026-06-09):** The early bench/motion/degeneracy runs used a placeholder IDENTITY LiDAR-IMU extrinsic — WRONG for this rig, caused yaw instability (±130° jumps) + "yaw sign inverted". Corrected to the rig's real Ouster extrinsic from the working FAST-LIO config: `extrinsicRotation_imu_laser=[-1,0,0,0,-1,0,0,0,1]` (OS1 IMU is yaw-flipped vs lidar), `T=[-0.006253,0.011775,0.028535]`, `use_imu_roll_pitch:true`. Replayed path now STABLE (confirmed in Foxglove). Key reusable fact: **for this rig's Ouster OS1-64, use extrinsic_R `[-1,0,0,0,-1,0,0,0,1]` — applies to EllipseLIO too** (its `r_imu_lidar`).

**Visualization:** SuperOdom builds the world in the sensor START orientation (not gravity-up like FAST-LIO's gravity_align_en) → map renders UPSIDE-DOWN. View via a 180°-roll `map_view` static TF (`ros2 run tf2_ros static_transform_publisher 0 0 0 1 0 0 0 map map_view`, follow map_view in Foxglove). For flight, `vision_to_mavros` must apply the world-Z/heading correction. Foxglove bridge: `apt install ros-humble-foxglove-bridge`, run on port 8765, connect from `ws://10.0.0.178:8765` (Jetson wifi).

**⚠️ Before any SuperOdom flight:** (1) world-Z/heading correction in vision bridge (not ENU/gravity-up); re-verify yaw sign on fresh motion test now extrinsic is fixed; (2) odom `pose.covariance` not populated.

**LIVE COMPUTE LIMITATION (2026-06-09, MEASURED — corrects earlier guesses):** Ran an instrumented A/B (per-core CPU via `/tmp/cpu_sampler.py`, per-process CPU, RAM, live `ros2 topic hz`). **Driver ALONE:** `/ouster/points` 19.4Hz steady, agg CPU 22% (os_driver 17.6% of one core), RAM 1.6GB. **Driver + SuperOdom:** `/ouster/points` drops to ~15Hz typical (variable 10–15.5, 3–5× jitter, max gap 0.2–0.5s); agg CPU 63% (cores 57–81%, **no core pegged, ~37% idle**); RAM only 1.8GB/24%; laser_mapping_node 79%, feature_extraction 73%, imu_preintegration 30% of-a-core; **os_driver rose 17.6%→27.7% yet delivers FEWER clouds**. **ROOT CAUSE FOUND + FIXED (2026-06-09): the LiDAR rate drop was the ROS2 MIDDLEWARE (default Fast-DDS / rmw_fastrtps_cpp), NOT compute. Switching to CycloneDDS restores a steady 20Hz.** This supersedes every earlier guess. Method = exhaustive elimination by direct live test, each ruled OUT: NOT RAM (24%); NOT single-core saturation (gave driver a dedicated core @62%, still 17Hz); NOT UDP (0 live UdpRcvbufErrors/10s; rmem_max already 64MB; the 1352 were STALE from the 2-instance+Foxglove overload); NOT `ros2 topic hz` artifact (reporter on dedicated core+nice-20, still 17Hz); NOT memory-bandwidth (EMC 0%); NOT thermal (66-70°C); NOT CPU clock (raised 1497->1984MHz via sysfs, CPU util DROPPED to ~50% but /ouster/points stayed ~15Hz — clock irrelevant to the drop); NOT reliable-QoS backpressure (both ends BEST_EFFORT); NOT the /points subscription (rate stayed ~13Hz with 0 subscribers). **DECISIVE ISOLATION: raw `/ouster/lidar_packets` held 1234Hz ROCK-STEADY under load while assembled `/ouster/points` collapsed — so the loss is in the driver's large-PointCloud2 ASSEMBLY/PUBLISH path, and it triggered merely from other SuperOdom DDS participants being alive. That is Fast-DDS large-message + multi-participant contention.** Proof: `RMW_IMPLEMENTATION=rmw_cyclonedds_cpp` on driver+SuperOdom -> **/ouster/points 20.0Hz steady (std 0.011s)**, sustained, temp 69.8°C @1984MHz. Under Fast-DDS the SuperOdom nodes also burned 60-90% CPU spinning on the transport; under Cyclone at full 20Hz throughput they do real work (feature 85%, mapping 90% @1984MHz — now CPU-bound but rate holds).

**REPLAY RE-VALIDATION (2026-06-09, corrected extrinsic + Cyclone, 1.0x):** Re-ran the degen_bag corridor walk through SuperOdom at NATIVE 1.0x (old run needed 0.5x). Tracked cleanly: path 62m, peak excursion 7.07m, return-to-start 0.84m (~1.4%), 4355 samples/174s. **The corrected extrinsic ELIMINATED the yaw blowup — end-yaw drift only -20.5deg vs the old identity-extrinsic ±130-137deg jumps.** Degeneracy signature unchanged (x/y/yaw uncertainty maxed, z best 0.52, icp 0.03->0.81); /state_estimation_health stayed TRUE 100% (0.0s false) — health-flag-insensitivity STANDS, still gate on uncertainty. Combined with the live driver+SuperOdom@Cyclone=20Hz measurement, SuperOdom is now real-time-capable at 1024x20 on this rig. Only a continuous LIVE motion test remains (replay proves processing+tracking; live co-run proves rate).

**⭐ LIVE MOTION TEST — PASSED (2026-06-10) — closes the last validation gap.** Continuous live ROS2 Ouster driver + SuperOdom co-running under CycloneDDS WITH real hand-carried motion (left turns ×2, a loop, a 45° line, return to exact origin+heading), `live_monitor.py` recording. RESULTS: **/ouster/points held 20.0Hz and /registered_scan 19.7Hz UNDER MOTION + co-run** (the previously-untested live-rate-under-motion piece — confirmed). **Loop closure: 0.07 m over a 20.18 m path (~0.35%)** — excellent live position tracking. **NO divergence: max single-scan step 0.08 m (~2 m/s)** — the ±13 m replay transient did NOT occur this run (one clean live loop; doesn't disprove the ~12% replay severe-rate, just didn't hit it; notably this was on a FRESH --init container). **YAW SIGN CONFIRMED INVERTED:** a physical LEFT turn reads NEGATIVE yaw (~−91° for 90°) — magnitude accurate, so it is PURELY a sign/frame convention issue → `vision_to_mavros` MUST negate yaw before MAVROS. **HEADING DRIFT ~15°:** returned to true start heading (0°) but estimate read −15° → ~15° yaw drift over the short loop (yaw is SuperOdom's weakest/least-observable DOF, consistent with the degeneracy study). Position is solid; HEADING is the flight-limiting axis. CAVEAT: ran at BOOSTED 1984MHz (sysfs override survived container recreate; nvpmodel mode 3). **Stock 25W/1497MHz live still UNTESTED.** Container was recreated WITH --init this session (tini PID1, zombies reaped to 0) as part of the container-state isolation experiment.

**CONTAINER-STATE ISOLATION (2026-06-10, in progress then preempted by live test):** Hypothesis: the transient-divergence rate correlates with accumulated container state (the no-init container had **155 zombie procs**; RAM/temp/clock all fine). Chronology of 25 valid replay runs showed anomalies cluster at high container-age: batch1 1/5, middle 20-batch stretch 0/14, dirtiest backfill 3/6 (50%). Built `scripts/state_correlation.sh` (logs zombies/RAM/temp before each run + outcome) and recreated the container WITH --init for a clean-vs-dirty A/B; the 15-run fresh batch was PREEMPTED when the user became available for live testing (higher priority). The single clean live run (max step 0.08m) is one fresh-container data point. TODO: resume `state_correlation.sh 15 superodom fresh` to finish the A/B (fresh-container rate vs the dirty 3/6) — decides container-artifact vs inherent nondeterminism.

**THE FIX (baked in): use CycloneDDS.** Added `ros-humble-rmw-cyclonedds-cpp` + `ENV RMW_IMPLEMENTATION=rmw_cyclonedds_cpp` to superodom_integration/Dockerfile; run_superodom_bench.sh exports it for BOTH driver and SuperOdom (they must match to interoperate). **This LIFTS the "SuperOdom not viable live at 1024x20" limitation — pending a live MOTION test** (stationary the nodes are in cold-start "throw laser scan" so tracking quality is unconfirmed; bench rate is now correct). 

**Secondary (CPU headroom / power mode) — NOW TESTED LIVE (2026-06-10): see [[jetson-cpu-clock-boost]].** A/B of the same hand-carried loop at stock 1497MHz vs boost 1984MHz: at **stock 25W, 20Hz HOLDS under motion but feature_extraction SATURATES a core (100-107%)** and the run showed a **1.10 m pose glitch + looser 0.17 m loop closure + a post-run dip to 19.25Hz**; at **boost 1984MHz it was clean (0.08 m max step, 0.07 m closure, steady 20Hz)**. **The boost costs only ~3°C** (69→72C; ~25C headroom remains) so thermal is NOT the limit — compute margin is. **User's call (confirmed): boost the clock for accuracy** (`sudo ~/superodom_ws/set_cpu_clock.sh boost`). Alternative if stuck at 25W: drop to 512x20. So 20Hz-at-25W is viable-but-marginal; prefer boost. Note `nvpmodel -m 0` (MAXN) is BROKEN on this module: the stock /etc/nvpmodel.conf MAXN entry references CORE_6/CORE_7 but the **Orin NX 8GB is a 6-core part** (16GB=8-core) -> `NVPM ERROR cpu6/online`. Workaround used: per-core sysfs `echo 1984000 > scaling_max_freq` + `performance` governor (non-persistent, reverts on reboot; needs sudo). For a persistent boost either fix the MAXN conf to 6 cores or add a boot unit writing scaling_max_freq. Modes: 0=MAXN(broken), 1=10W, 2=15W, 3=25W. SuperOdom still won't track stationary (feature_extraction "throw laser scan, only at the beginning" until motion). So **at 1024x20 live SuperOdom degrades the LiDAR but the cause is scheduling, not compute exhaustion** — worth trying driver core-pinning + RT prio + 512x20 before declaring it unflyable. Validated offline method unchanged: bag-record (driver only, 20Hz) + offline replay 0.5x (`~/superodom_ws/degen_bag`, 10GB, gitignored). Use lighter **EllipseLIO for live** as the lower-risk path.

**IMAGE BUG — PARTIALLY fixed, STILL BROKEN (corrected 2026-06-10):** `superodom:humble` (sha 610cd9b) has `libzip.so.4` baked in (fixed the os_driver `libzip.so.4` SIGABRT), BUT it is **MISSING `ros-humble-rmw-cyclonedds-cpp` AND `ros-humble-foxglove-bridge`** even though the Dockerfile lists both (lines 109-110). The earlier "verified cyclone+foxglove baked in" claim was WRONG — the rebuild cache-skipped that apt layer (lines 109-110 were added but a cached layer from before them was reused). PROOF (2026-06-10): a fresh container from this image crashed the driver with `librmw_cyclonedds_cpp.so: cannot open shared object file`; `docker run --rm superodom:humble` shows cyclone .so MISSING + `ros2 pkg prefix foxglove_bridge` MISSING (libzip present). **The old (pre-recreate) container had cyclone+foxglove RUNTIME-installed — that's why this session's bag replays worked; the IMAGE never had them.** **FIXED 2026-06-10 by `DOCKER_BUILDKIT=1 docker build --platform linux/arm64 -t superodom:humble .`** — because lines 109-110 are now part of the L104-111 apt RUN string (which differs from the 610cd9b-era string that had only libzip), the cache for that one layer invalidated and re-ran (downloaded cyclone+foxglove+rmw-cyclonedds-cpp), while the heavy GTSAM/Sophus layers above stayed cached → fast build. **New image sha 1cb662a VERIFIED:** `docker run --rm superodom:humble` shows cyclonedds .so OK + foxglove_bridge pkg OK + libzip OK. NOTE: the currently-RUNNING container predates this and has cyclone runtime-installed but NOT foxglove — recreate it from the new image (`run_superodom_bench.sh`, which now has --init) to pick up foxglove + the baked-in fix.

**DEGENERACY findings (offline replay, 54.67m featureless corridor, `superodom_integration/results/DEGENERACY_FINDINGS.md`):** drift ~0.79m (~1.4%); uncertainty_x maxed ~94%, uncertainty_yaw maxed ~always (yaw = weakest DOF, jumped ±130° at far end), z best-constrained. **`/state_estimation_health` stayed TRUE the entire degenerate run — the boolean health flag does NOT detect degeneracy.** (`plane_match_success` counter reads 0 even when tracking.)

**⭐ 5× REPEATABILITY + STATS-USABILITY SWEEP (2026-06-09, `scripts/repeatability.sh` + `scripts/analyze_degeneracy.py`, CSVs in `~/superodom_ws/results/repeat/`) — REVISES the "gate on uncertainty" advice above; THIS is the authoritative gating guidance.** Ran degen_bag 5× identical (1.0×, Cyclone). **#1 Endpoint repeatability excellent: net return-to-origin 0.85/0.91/0.87/0.87/0.87m → mean 0.87m, sd 0.02m; end-yaw ±1°.** (net = drift + the operator's real end-offset → an UPPER BOUND; true drift is smaller — user noted they didn't stop exactly at origin.) **BUT 1 of 5 runs (run 4) had a crash-grade transient: pose OSCILLATED ±13m between consecutive scans for ~0.7s @t≈112s then recovered to a normal endpoint (net 0.87m, path 251m vs ~63m, peak 13.2m vs ~7.2m). Same input, nondeterministic. Endpoint error HID it completely — never validate this stack on return-to-origin alone; track peak excursion + path length too.** **#3 NO `/super_odometry_stats` field is a usable EKF-gating signal in this build:** health stuck true through the explosion; `uncertainty_x/y/yaw` SATURATED ~1.0 from startup in good AND bad runs (zero discrimination — so the earlier "gate on uncertainty" is WRONG for this build); `plane_match_success`=0 every scan; `latency` large NEGATIVE (clock mismatch); `n_iter` pinned at cap 5; `translation_from_last`=0.03m WHILE the pose jumped 10m. The transient is invisible in every self-reported metric. **→ GATE EXTERNALLY on a kinematic plausibility check of `/state_estimation` itself: run 4's jumps were ~10m in ~0.02s ≈ 500 m/s — reject any pose implying >5–10 m/s. Do NOT trust health OR uncertainty.**

**⭐ FAST-LIO vs SuperOdom HEAD-TO-HEAD — on CLEAN data SuperOdom WINS (2026-06-11, corrected).** Two bags, both boosted clock + CycloneDDS, via the ROS2 `FAST_LIO_GPU` in `slam_gpu_system`/`slam-gpu:latest` (NOT the FAST-LIVO2 container, stays OFF); `fast_lio_gpu.yaml` has the OS1 extrinsic `R=[-1,0,0,0,-1,0,0,0,1]`, `/ouster/points`+`/ouster/imu`, lidar_type 3.
- **`mixed_route` (had a 7.8s SENSOR DROPOUT, see [[lidar-field-recording-dropout]]):** BOTH diverged on resume — not a fair test. FAST-LIO *looked* smoother in the pre-dropout window, which led to a WRONG early "FAST-LIO is the clear winner" call. **Retracted.**
- **`apartment` (CLEAN, validated — 153s feature-rich indoor loop, the proper benchmark):** **SuperOdom decisively better. SuperOdom: loop closure 0.13m over 96m path, max single-step 0.20m, ZERO divergence — excellent.** FAST-LIO: drifted throughout (read 15.5m from origin at t=147s when SuperOdom's TRUE peak excursion was only 9.6m → real accumulated drift/scale error), then a smooth +z/+x IMU runaway in the final ~5s (→92m). So on clean, easy indoor data FAST-LIO both drifts more AND diverged.

**FAST-LIO's apartment failure was a CONFIG BUG, now FIXED (2026-06-11) — both frameworks are viable on clean data.** Investigated the FAST-LIO runaway: it was NOT pervasive drift — FAST-LIO tracked the whole loop within ~2-3m of SuperOdom (frame-convention rotation aside) then ran away ONLY in the final ~6s (smooth +z IMU runaway 14→92m) while the user stood still at origin. Streams were clean/continuous (no early end, 20Hz to 153s), 0 "No Effective Points" — a real divergence on good data. **Root cause: `extrinsic_T: [0.0,0.0,0.0]` in `slam-gpu/config/fast_lio_gpu.yaml` ignored the real ~3cm OS1-64 LiDAR→IMU lever-arm.** FIX (applied to that config): `extrinsic_T: [-0.006253, 0.011775, 0.028535]` (the working SuperOdom value) + `extrinsic_est_en: true` (online extrinsic calib, was false). Re-benchmark: FAST-LIO max single-step **5.89m→0.14m**, loop closure **92m→1.85m**, peak excursion now **9.55m vs SuperOdom 9.60m (agree to 5cm)** — runaway GONE.
**REVISED VERDICT (final): on this rig's CLEAN data BOTH SuperOdom and FAST-LIO track well** — SuperOdom tighter loop closure (0.13m vs 1.85m), FAST-LIO smoother per-scan (0.14m vs 0.20m max step). The earlier catastrophic divergences were (a) the sensor dropout [[lidar-field-recording-dropout]], and (b) FAST-LIO's wrong extrinsic_T — NEITHER an inherent framework weakness. **Always set the real `extrinsic_T` for FAST-LIO on this rig; `[0,0,0]` looks fine for most of a run then tips into an end-of-run IMU runaway.** SuperOdom's own ~12% nondeterministic transient (corridor study) remains its one known weakness but didn't fire here. **Comparing the two frameworks' raw x,y,z is invalid — they use different world-frame conventions (SuperOdom = sensor-start orientation + inverted yaw; FAST-LIO = gravity-aligned); compare frame-INVARIANT metrics (peak excursion, path, loop closure, max step).** Harness in `superodom_integration/scripts/`: `run_fastlio.sh`, `benchmark_bag.sh` (BOTH + comparison), `fastlio_monitor.py`. Gotcha: slam-gpu `config/` mount is READ-ONLY → bench config (map_en:true, else `/Laser_map` silent) generated into writable `bags/`.

**⚠️ ROOT CAUSE of that bag's divergence was NOT the algorithms — it was a 7.8s SENSOR BLACKOUT in the recording (both `/ouster/points` AND `/ouster/imu` gapped together at t=76s).** See [[lidar-field-recording-dropout]]. Both frameworks tracked fine until the blackout, then diverged on resume (unavoidable for any odometry). The clean comparison window is therefore 0–76s. Always run `scripts/check_bag_gaps.py <bag>` before trusting a field bag for benchmarking.

**Profile** `validated: true, integration_complete: false`. EllipseLIO still un-built (`validated: false`) — the lower-risk option if SuperOdom's heavy build/yaw issues bite.

**⭐⭐ EllipseLIO PHASE 5 LIVE VALIDATED — 2026-06-21 (closes the last EllipseLIO gap; profile now `validated:true`).** Freed the Ouster (`docker stop slam_gpu_system` — it was idle FAST-LIO, no driver feeding it), CLOCK BOOSTED to 1984MHz (user ran `sudo ~/superodom_ws/set_cpu_clock.sh boost`; needs sudo, agent can't), ran `ellipselio_integration/scripts/run_ellipselio.sh` (live Ouster driver + EllipseLIO + viz + foxglove, all CycloneDDS). **Bench: `/ouster/points` 20.02Hz steady (std 0.0045s), `/ellipselio_odom` ~79Hz (IMU-rate forward-prop), SINGLE publisher (no dup-estimator leak), valid near-origin pose.** Hand-carried motion test #2 (translate forward/back/left/right + lift + **90deg CCW then 90deg CW**, return to origin, sampled via `ellipselio_traj_sampler.py` on `/ellipselio_odom`): **loop closure 0.02m (2cm return!), max single-step 0.01m (ZERO divergence, smoothest measured on this rig), yaw drift -3.2deg (168.0->164.8, far better than SuperOdom's ~15deg), peak 1.18m, path 7.22m, NO terminal stationary runaway** (stationary periods held put — the bag's terminal runaway did NOT fire live). Test #1 (translate-only) corroborated: 0.19m closure, 0.02m max step. **⚠️ YAW SIGN INVERTED (confirmed, same as SuperOdom): a physical CCW (left) turn reads as yaw DECREASE — baseline 168deg dropped to min 77.4deg (Δ≈-90.6deg) on the 90 CCW, then back to 175 on the CW. Magnitude accurate (~90deg). `vision_to_mavros` MUST NEGATE YAW before MAVROS.** CAVEAT: this was a ~1m handheld envelope, NOT a flight-scale 15-20m walking loop — estimator is proven live but `integration_complete:false` until a flight-scale loop + vision bridge (negate yaw + world-Z/heading) + GPS-denied flight. Results in `~/ellipselio_ws/results/live/motion_test{,_2}.csv`.

**⭐⭐ 3rd_floor RUNAWAY DIAGNOSIS (2026-06-21) — `obs_score` is NOT a flyaway gate; runaway is SILENT in analytics.** Replayed 3rd_floor through EllipseLIO (boost, CycloneDDS, fresh container per run via `run_bag_foxglove.sh` = bag replay + Foxglove + traj sampler + `ellipselio_diag.py` logging `/analytics`). 8 runs today: **~2/8 (~25%) runaway, lower than the historical ~50%** (the fresh-container-per-run clean slate seems to help). KEY METHOD FIX: `disp_from_start` is NOT pure error — 3rd_floor is a real ~58m floor walk, so peak_excursion ~58m is REAL motion; the ONLY error signal is `final_disp_from_start` (operator returns sensor to origin, so final>3m == failed closure == runaway). Clean runs close to **0.01-0.02m, max-step <0.15m**; runaways end >4m. **Two DISTINCT runaway mechanisms, both smooth (no jump): (1) run-1 closed to ~0.03m at origin then DRIFTED ~9m in Y while held STATIONARY (the terminal-stationary runaway, ~9m over final 20s); (2) run-6 FAILED TO CLOSE by ~4m during the return walk then held steady at 4m (loop-closure error, not stationary drift).** ⭐ **THE FINDING: `ellipselio_diag.py` correlation of a RUNAWAY run (run6, 4.13m) vs a CLEAN run (run2, 0.01m) shows analytics are NEARLY IDENTICAL through the whole run — and at the endpoint the RUNAWAY had HIGHER obs_score (mean 0.98/min 0.72) than the CLEAN run (0.95/0.39); num_feats (4145 vs 4287), rng_mean (both 800-1500m), num_reject all indistinguishable.** So `obs_score` IS a real dynamic signal (dips 0.26-0.55 in feature-poor/high-rng zones, unlike SuperOdom's stuck-true health flag) BUT it does NOT detect/predict the runaway — the divergence is a silent nondeterministic EKF/tensor-voting error invisible in every self-reported metric. SAME lesson as SuperOdom: **the estimator's own confidence canNOT gate its flyaway.** And because the runaway is SMOOTH + GRADUAL (max-step 0.10m, ~4m accumulated over the return), even an external velocity/kinematic gate (>5 m/s reject) would MISS it — only an ABSOLUTE reference (fiducial / prior-map relocalization) catches the position discrepancy. **Direct evidence for the Hangar-737 absolute-anchor requirement: degeneracy/confidence gating alone is insufficient; you need an absolute anchor.** Data: `~/ellipselio_ws/results/field/3rd_floor_*_runaway_hunt/run{1..6}_{analytics,trajectory}.csv`. Tools: `run_bag_foxglove.sh` (watch+analyze, /analytics logged), `loop_until_runaway.sh` (re-run until final_disp>thresh), `ellipselio_diag.py` (obs_score/feats vs disp/step). EllipseLIO saliency viz: `/visualization_marker` MarkerArray (plane=blue/line=green/ball=yellow ellipsoids, lifetime=0 accumulate) — whitelist it + `/cloud_effected` (features used in EKF update) in foxglove_params.yaml.

**FOXGLOVE FRAME-ORIENTATION FIX (2026-06-21) — interim hack THEN proper fix:** EllipseLIO stamped ALL clouds/odom/TF in frame `/odom_ellipselio` (LEADING SLASH, hardcoded `src/map_processing.cpp` as `node_namespace_ + "/odom_ellipselio"`, default empty namespace). The slash is non-canonical: `static_transform_publisher` strips it so a view-rotation frame can't attach, and Foxglove won't list/apply such frames.
- INTERIM HACK (works against UNpatched ellipselio): bake the 180deg rotation into the cloud DATA. `cloud_throttle.py`+`odom_to_path.py` gained a mode arg (`native|rollX|pitchY|yawZ`, negate matching XYZ axes). But this only rotates the clouds/path, NOT the TF frame axes (imu_ellipselio/imu_prop_ellipselio) — they stayed in the raw frame and moved opposite. Kept only as a fallback.
- **PROPER FIX (this rig, ellipselio now patched): (1) drop the leading slash in `map_processing.cpp` (9 frame_id/child_frame_id literals -> `node_namespace_ + "odom_ellipselio"` etc; topics unchanged), rebuild `--packages-select ellipselio` (~1.5min incremental, branch `fix/frame-names-no-leading-slash` 4e010b9 in `~/ellipselio_ws/src/ellipselio`, origin=upstream v4rl-ucy so LOCAL-only — push to a fork to persist remotely). (2) Then a `/tf_static` `map_view -> odom_ellipselio` 180deg PITCH (flips X,Z) parents EllipseLIO's whole tree, so cloud+path+imu frames ALL render upright together. Set Foxglove Fixed Frame = map_view.** PITCH not roll: a 180 X-roll stands it upright but leaves X pointing BACKWARD; EllipseLIO's native frame is X-backward AND Z-down, so 180 PITCH (flip X,Z, keep Y) fixes both. Two gotchas: (a) use `/tf_static` (TIMELESS — applies at any msg stamp, transforms sensor-stamped clouds correctly); a continuous `/tf` broadcast stamped with the NODE clock breaks the chain (time-base mismatch vs EllipseLIO's sensor-time TF) — symptom: `tf2_echo map_view odom` works but `map_view -> imu_prop` won't resolve. (b) Foxglove CACHES `/tf_static` client-side — after CHANGING a static rotation you must reconnect the client to see it. Display-only; `/ellipselio_odom` untouched (metrics stay frame-invariant). slam-agent commit da5ca69 (branch feat/ellipselio-phase5-live).

**⭐⭐ 3-WAY OPTIMIZATION PASS (2026-06-11) — all three tuned + re-benchmarked on the apartment bag @ boost (1984MHz), 2 runs each.** FINAL tuned results (loop closure / peak excursion / max single-step; true path ~63m, all peak ~9.5-10m = same real path):
| Framework | loop closure | peak | max-step | diverge? (2 runs) |
|---|---|---|---|---|
| **EllipseLIO** (map_res 0.2, min_range 0.3, max_range 80) | **0.00 / 0.00 m** | 9.65-10.17 | **0.06-0.07** | NONE |
| **FAST-LIO** (extrinsic_est_en **false**) | 0.96 / 1.27 m | 9.55-9.57 | 0.17-0.18 | NONE |
| **SuperOdom** (filter_point_size 4, max_surface 1500) | 0.19 / 0.19 m | 9.56-9.83 | **9.10-12.56** | transient BOTH runs |
**Ranking on THIS clean bag: EllipseLIO (tuned) wins** — perfect loop closure, smoothest, no divergence — then FAST-LIO (clean ~1m), then SuperOdom (best endpoint recovery 0.19m but its compute-transient fires every run). Caveats: one bag; EllipseLIO needs boost (compute-marginal at stock) and its starvation vuln is mitigated-not-eliminated; SuperOdom transient is the standout weakness this session.

Three optimization findings (each tuned + re-tested 2x @ boost):
1. **EllipseLIO terminal runaway = FEATURE STARVATION, fixed by COARSENING the map.** Diag (`ellipselio_diag.py` correlating /analytics with odom) showed the runaway onset coincides EXACTLY with feats collapsing 1000→<50, **obs_score 1.0→<0.5**, ~all points rejected, rng_mean spiking to 100-430m (matching spurious far points). Root cause: `map_resolution 0.1` was FINER than FAST-LIO (0.5) / SuperOdom (0.2) → fragmented the few features at sparse spots → tensor-voting collapse → IMU runaway. **Fix `map_resolution 0.1→0.2` (+min_range 0.5→0.3, max_range 100→80): loop 0.00m, no runaway, 2/2 @ boost.** At STOCK 1497 it still diverged ~50% (compute-marginal) → **boost is part of the fix.** `obs_score` is a usable NATIVE degeneracy gate (unlike SuperOdom's useless health flag).
2. **SuperOdom transient is COMPUTE-LOAD-CORRELATED (not random).** Monotonic across 3 compute levels: heavy(iter8/surf3000)=60m, baseline(iter5/surf2000/filter3)=20m, **light(iter5/surf1500/filter4)=9-12m** max-step. Lighter per-scan load → more real-time margin → smaller transient. Loop closure unchanged (0.19m), peak excursion CORRECTED (9.6 vs baseline's inflated 11m). Container was clean (--init, 0 zombies) so this is NOT the zombie hypothesis [[new-slam-frameworks-2026-06]] note — it's genuine compute marginality at 1024x20. **⭐ RUN-3 (2026-06-11) CLINCHES IT — the transient VANISHES under true CPU isolation.** Re-ran the 3-way with the competing live EllipseLIO node KILLED before each replay (so only the framework-under-test had the 6 cores): **SuperOdom max-step 9-12m → 0.23m, loop closure 0.13m, NO transient (first clean SuperOdom run).** The only change was removing a concurrent CPU consumer. This upgrades the finding from "lighter config shrinks the transient" to **"the transient is CPU-CONTENTION-driven; given the cores to itself SuperOdom is stable."** OPERATIONAL: for flight, SuperOdom must NOT share the Jetson with another LIO/heavy node, AND keep the kinematic gate as a safety net (one clean run ≠ proof it can never fire). The earlier 2/2-fired runs were confounded by a co-running EllipseLIO node eating CPU.
3. **FAST-LIO: the real fix was `extrinsic_T`, NOT `extrinsic_est_en:true` — est_en:true was HARMFUL.** With `extrinsic_est_en:true` (the old "fix") FAST-LIO diverged CATASTROPHICALLY this session (9044m, only 2627 samples) at the SAME feature-poor spot (~t=84-90). With a CORRECT extrinsic_T, online est adds a free DOF that drifts during degeneracy. **`extrinsic_est_en:true→false`: loop 0.96-1.27m, max-step 0.17m, full 3056 samples, NO divergence, 2/2 — and BEAT the old 1.85m.** Correcting the earlier memory: est_en:true got lucky through the starvation spot ONCE; it is not reliable.

**⭐⭐ RUN-3 (2026-06-11) — REPEAT OF THE 3-WAY WITH CPU ISOLATION (live competing node killed before each replay), all @ boost, apartment bag.** This is the cleanest comparison yet (each framework had all 6 cores):
| Framework | path | peak | loop closure | max-step | diverge? |
|---|---|---|---|---|---|
| **EllipseLIO** (tuned) | 62.73 | 9.65 | **0.01 m** | **0.13 m** | NONE |
| **FAST-LIO** (est_en false) | 70.60 | 9.49 | 0.32 m | 0.27 m | NONE |
| **SuperOdom** (tuned) | 95.95 | 9.58 | 0.13 m | **0.23 m** | **NONE** ✓ |
**ALL THREE clean, no divergence.** EllipseLIO again wins (0.01m loop, smoothest; path 62.73≈true 63m — SuperOdom's 95.95m path is integrated jitter, not extra distance). **The headline: SuperOdom's transient did NOT fire** (vs 9-12m both runs in the 2-run pass that had a co-running EllipseLIO node) → see finding #2 above, CPU-contention is the cause. FAST-LIO marginally looser this run (0.32m loop, est_en:false) but rock-solid. Methodology lesson baked into the harness order: **run frameworks SEQUENTIALLY and kill all other LIO nodes first** — concurrent runs confound the compute-marginal ones (SuperOdom especially).

**⚠️ THE APARTMENT BAG HAS A FEATURE-POOR SECTION ~t=84-90s that challenges ALL THREE frameworks** (EllipseLIO feats-collapse, FAST-LIO divergence w/ est_en, SuperOdom transient all cluster there). It's a bag property, not a single-framework flaw. Each framework's robustness to it varies run-to-run. Tuned configs (above) survive it; baseline/mis-tuned configs diverge there.

Tuned config locations: EllipseLIO `ellipselio_integration/config/os1_64_ouster.yaml` + ws copy; SuperOdom `superodom_integration/config/os1_64.yaml` + `~/superodom_ws/src/SuperOdom/super_odometry/config/os1_64.yaml`; FAST-LIO `~/slam-gpu/config/fast_lio_gpu.yaml`. All annotated with rationale. Harnesses: EllipseLIO `run_bag.sh`+`ellipselio_traj_sampler.py`+`ellipselio_diag.py`; SuperOdom `process_field_bag.sh`; FAST-LIO `run_fastlio.sh`.

**⭐ EllipseLIO APARTMENT BENCHMARK (2026-06-11) — tracks the loop as well as FAST-LIO, but TERMINAL stationary-endpoint runaway.** Replayed the clean `apartment_20260611_034239` bag (153s, 20Hz, the validated benchmark) through EllipseLIO via new `ellipselio_integration/scripts/run_bag.sh` (recreates `ellipselio` ctr with `-v ~/superodom_ws/field:/root/field:ro`, `use_sim_time:=true`, `ros2 bag play --clock`, standalone `ellipselio_traj_sampler.py` on `/ellipselio_odom`). **CLEAN-WINDOW result (t<144s, the actual loop): loop closure 1.82m / peak excursion 9.66m / max single-step 0.09m / path 62.75m** — peak excursion AGREES with SuperOdom (9.60) & FAST-LIO (9.55) to <0.1m (same real path); loop closure = FAST-LIO's 1.82≈1.85 (both FAST-LIO lineage; SuperOdom tighter at 0.13); **SMOOTHEST per-scan of the three (0.09 vs FAST-LIO 0.14, SuperOdom 0.20).** So on clean apartment data EllipseLIO is competitive/excellent. **BUT a smooth +x/+z IMU runaway begins ~t=144 (standing still at the feature-poor origin endpoint) → 28.7m by bag end** — the SAME failure mode FAST-LIO showed on this exact bag PRE-extrinsic-fix. NOT a stream dropout (both topics feed full-length to 153.3s). EllipseLIO has **NO online-extrinsic / extrinsic_est flag** (confirmed via grep; "weights self-adapt, no knobs") so FAST-LIO's `extrinsic_est_en:true` fix is unavailable. Open: is the terminal runaway tunable (try `min_range` 0.5→0.0, IMU noise, or confirm endpoint is genuinely feature-poor via `/analytics.num_feats`) or inherent FAST-LIO-family stationary sensitivity? SuperOdom (non-FAST-LIO lineage) did NOT run away here. **⚠️ FOR FLIGHT: gate EllipseLIO odom on kinematic plausibility (same as SuperOdom) — the terminal runaway would feed MAVROS a flyaway.**

**⚠️⚠️ CRITICAL ELLIPSELIO CONFIG BUG FOUND + FIXED (2026-06-11): `lidar.rate` MUST match the real scan rate (20 for this rig's 1024x20), NOT the NCD dataset's 10.** First apartment replay diverged CATASTROPHICALLY (8000m runaway) because `os1_64_ouster.yaml` had `rate: 10` (copied from `os64_ncd.yaml`, whose Ouster ran at 10Hz). EllipseLIO uses `rate` for IMU↔LiDAR association (`imu_processing.cpp:170 match_idx=floor(time_diff*rate)`) AND deskew period (`map_processing.cpp:11 scan_time=1/rate`) — a 2× error mis-associates IMU to scans → EKF runs away under motion. **Fix `rate: 20` → clean tracking** (the 1.82m result above). Lesson: when porting an EllipseLIO dataset config, ALWAYS set `lidar.rate` to the live sensor's real Hz. Also note EllipseLIO publishes `/ellipselio_odom` as **BEST_EFFORT** QoS — a RELIABLE subscriber gets ZERO messages (the sampler must use BEST_EFFORT; `ros2 topic hz` masks this by auto-negotiating).

**⭐ EllipseLIO CONTAINER BUILT (2026-06-11) — builds clean, NOT yet bench/flight-tested.** Mirrored the SuperOdom container pattern but FAR lighter (no GTSAM/Ceres/Sophus/Livox — IKFoM/i-Octree vendored): image `ellipselio:humble` (arm64, **3.3GB** vs superodom's heavy image) from `ellipselio_integration/Dockerfile` (`ros:humble-ros-base`, OpenMP+PCL+Eigen+Ouster-driver deps, **CycloneDDS baked as default rmw** — same Fast-DDS large-cloud lesson). Host ws `~/ellipselio_ws/` (ellipselio main `8b43d70` + ouster-ros reused from superodom_ws). `colcon build --packages-up-to ouster_ros ellipselio` SUCCEEDED: `ellipselio_mapping_node` + full `os_driver`/`os_sensor`/`os_cloud` set in install/ (ellipselio had only a benign GCC C++17 param-passing note; ouster only deprecation warnings). **EllipseLIO config repointed to the LIVE rig:** `ellipselio_integration/config/os1_64_ouster.yaml` (also symlinked into the pkg's `config/`) — `/ouster/points`+`/ouster/imu`, `vertical_fov: 42.4` (tune-critical OS64), and the rig's confirmed OS1 extrinsic (`r_imu_lidar=[-1,0,0,0,-1,0,0,0,1]`, `t=[-0.006253,0.011775,0.028535]`) — same as the corrected SuperOdom/FAST-LIO value. Bringup: `ellipselio_integration/scripts/run_ellipselio.sh` (container→Ouster driver→EllipseLIO, `use_sim_time:=false` MANDATORY live), build helper `scripts/build_ws.sh`, README documents it all. **No-sensor validation passed** (pkg/exe/launch/config all resolve, rmw=cyclonedds). **REMAINING: Phase 5** — free the Ouster (`docker stop superodom slam_gpu_system`), run live, verify `/ellipselio_odom` rate, then bench/motion test like SuperOdom. Profile stays `validated:false` until that runs. Not committed (new `ellipselio_integration/` dir, untracked).

**MRS UAV System — now added as an agent OPTION** (`references/mrs_uav_system.md` + SKILL.md rule 10). User wants ALL MRS capability available as a selectable stack. MRS is PX4-only + ROS 2 Jazzy/24.04-only → selecting it forces autopilot=PX4, ros=jazzy, docker=true and supersedes Phase 8. Assessment + 5 go/no-go gates at `docs/MRS_PX4_MIGRATION_ASSESSMENT.md`. User leaning "switch to PX4"; recommended time-boxed SITL/Docker spike first, keep ArduPilot baseline. Adopt PX4+MRS only for the full control/planning/sim stack, not just confined-space SLAM. See [[fast_livo2_camera_selection]] for the rig.

Work committed on branch `feat/superodom-ellipselio-mrs` (3 commits: frameworks+MRS-scoping, fastlivo2 WIP, Phase-2-validation+MRS-option). Not pushed.
