# Generated by SLAM Integration Agent — Phase 2 (2026-06-06)
# Profile: jetson_orin_nx_8gb-ouster_os1_64-fast_livo2-ardupilot-noetic
ros_version: "ROS1"
ros_distro: "noetic"
workspace_path: "~/catkin_ws"      # built inside the Docker image
flight_controller: "ardupilot"
use_dds: false
lidar_type: "ouster"
camera_type: "none"                # deferred — global-shutter + ext-trigger pending
slam_algorithm: "fast_livo2"       # LiDAR-inertial-only (img_en=0) until camera arrives
docker: true

# Platform-specific carry-forward notes for install/build:
notes:
  - "8GB Orin NX (6-core): tune LiDAR mode (1024x20 needs point_filter_num:4 + filter_size:0.5, else 512x20)"
  - "Use pre-built Ouster ROS1 driver from ~/ultraviewdev_ws (stock ouster-ros nodelets crash exit 255 on ARM64)"
  - "Bake OpenCV ABI fix (LD_PRELOAD + link order) into image now for when vision is enabled"
  - "Ouster link-local 169.254.56.220 vs host eth0 192.168.2.50 — align networking before data flows"
  - "FC stable path: /dev/serial/by-id/usb-Hex_ProfiCNC_CubeOrange-bdshot_450035000851313231383238-if00"
