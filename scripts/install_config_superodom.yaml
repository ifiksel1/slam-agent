# Generated by SLAM Integration Agent — Phase 2 (2026-06-09)
# Profile: jetson_orin-ouster_os1_64-super_odometry-ardupilot-humble
# Verdict: COMPATIBLE via Docker only.
ros_version: "ROS2"
ros_distro: "humble"
workspace_path: "~/ros2_ws"        # built INSIDE a Humble Docker image
flight_controller: "ardupilot"
use_dds: false                     # MAVROS path (vision_to_mavros), not uXRCE-DDS
lidar_type: "ouster"
camera_type: "none"                # SuperOdom is LiDAR+IMU only — NO camera needed
slam_algorithm: "super_odometry"
docker: true                       # MANDATORY — host is Ubuntu 20.04, no native Humble

# Platform-specific carry-forward notes for install/build:
notes:
  - "Host is Ubuntu 20.04/focal (ROS Noetic only). ROS 2 Humble exists only in Docker (ros:humble, ros2-jetson-humble images present). Build SuperOdom into a Humble image."
  - "Orin NX 8GB / 6-core: AT SuperOdom's 8GB RAM floor. Build GTSAM@4abef92 with -j4 + >=4GB swap (MARCH_NATIVE=OFF mandatory). Sophus@97e7161 (reuse scripts/patch_sophus_cmake.py + SophusConfig.cmake)."
  - "livox_ros_driver2 is a HARD build dep even for Ouster-only; also needs rviz_2d_overlay_plugins. Build Livox SDK2 first."
  - "Ouster OS1-64: laser_topic /ouster/points, imu_topic /ouster/imu, scan_line 64 (from os1_128 config). ouster-ros already built in ~/super_ws."
  - "Bridge: /state_estimation (nav_msgs/Odometry) -> vision_to_mavros -> /mavros/vision_pose/pose, GATED on /state_estimation_health (Bool). Do NOT trust odom pose.covariance (unpopulated)."
  - "Separate OpenCV calibration YAML (calibration_file arg) — measure LiDAR-IMU lever arm."
  - "FC: Cube Orange @ /dev/ttyTHS1 921600 (or by-id path), MAVROS, ArduPilot EK3 SRC=ExternalNav."
  - "CAUTION: ~/super_ws is the SUPER planner, NOT SuperOdometry — use a fresh ~/ros2_ws or Docker workspace."
